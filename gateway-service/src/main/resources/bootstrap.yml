spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      server-addr: 13.230.242.138:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: jwt-config.yaml
            refresh: true
          - data-id: spring-config.yaml
            refresh: true
