spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 47.93.248.2:8848
      config:
        file-extension: yaml #文件后缀名
        shared-configs:
          - data-id: redis-config.yaml
            refresh: true
