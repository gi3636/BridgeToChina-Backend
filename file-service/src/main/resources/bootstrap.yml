spring:
  application:
    name: file-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 13.230.242.138:8848
      discovery:
        namespace: dev
      config:
        file-extension: yaml #文件后缀名
        shared-configs:
          - data-id: spring-config.yaml
            refresh: true



