spring:
  application:
    name: file-service
  cloud:
     nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
      config:
        file-extension: yaml #文件后缀名
        shared-configs:
          - data-id: spring-config.yaml
            refresh: true



