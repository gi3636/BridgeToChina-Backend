spring:
  application:
    name: file-service
  cloud:
     nacos:
      config:
        namespace: #spring.profiles.active#
        file-extension: yaml #文件后缀名
        shared-configs:
          - data-id: spring-config.yaml
            refresh: true



